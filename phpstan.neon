includes:
    - vendor/szepeviktor/phpstan-wordpress/extension.neon

parameters:
    level: 5
    paths:
        - src/
    bootstrapFiles:
        - vendor/autoload.php
    ignoreErrors:
        # @return T|null PHPDoc on generic method — PHPStan cannot verify T extends object here.
        - identifier: return.phpDocType
